{
  "name": "@serverhub/plugin-cloudflare",
  "version": "2.0.0",
  "description": "Cloudflare DNS, CDN, WAF, SSL certificate, and Tunnel management",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@serverhub/plugin-sdk": "^1.0.0"
  },
  "devDependencies": {
    "@serverhub/plugin-cli": "^1.0.0",
    "typescript": "^5.3.0"
  }
}
